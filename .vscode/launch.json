{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug nano_binary (CodeLLDB)",
            "program": "${workspaceFolder}/nano_binary",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build-and-compile"
        }
    ]
}
